# Scenario 04 — independent chores

**User message:**

---

Split into PRs:

```
M  README.md
M  apps/web-app/vite.config.ts
M  .gitignore
```

Assume: README typo fix, Vite server port option for local dev, `.gitignore` adds `*.local.env`. No shared imports between files.

---
